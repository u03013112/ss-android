<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.github.ss"
    tools:ignore="MissingLeanbackSupport">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <application
        android:name="com.github.ss.App"
        tools:ignore="MissingTvBanner">
        <activity android:name="com.github.ss.JNewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity-->
<!--            android:name=".JActivity"-->
<!--            android:label="@string/app_name"-->
<!--            android:launchMode="singleTop"-->
<!--            android:theme="@style/Theme.Shadowsocks.Immersive.Navigation">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->
<!--        <activity android:name=".StoreActivity" /> &lt;!&ndash; <activity &ndash;&gt;-->
        <!-- android:name=".MainActivity" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:launchMode="singleTop" -->
        <!-- android:theme="@style/Theme.Shadowsocks.Immersive.Navigation"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- <category android:name="android.intent.category.LEANBACK_LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" /> -->
        <!-- </intent-filter> -->
        <!-- <meta-data -->
        <!-- android:name="android.app.shortcuts" -->
        <!-- android:resource="@xml/shortcuts" /> -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".ProfileConfigActivity" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- android:label="@string/profile_config" -->
        <!-- android:theme="@style/Theme.Shadowsocks.Immersive" /> -->
        <!-- <activity -->
        <!-- android:name=".AppManager" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- android:label="@string/proxied_apps" -->
        <!-- android:parentActivityName=".ProfileConfigActivity" -->
        <!-- android:theme="@style/Theme.Shadowsocks.Immersive" /> -->
        <!-- <activity -->
        <!-- android:name=".UdpFallbackProfileActivity" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- android:label="@string/udp_fallback" -->
        <!-- android:theme="@style/Theme.Shadowsocks.Immersive" /> -->
        <!-- <activity -->
        <!-- android:name=".ScannerActivity" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- android:label="@string/add_profile_methods_scan_qr_code" -->
        <!-- android:parentActivityName=".MainActivity" /> -->
        <!-- <activity -->
        <!-- android:name=".tasker.ConfigActivity" -->
        <!-- android:theme="@style/Theme.Shadowsocks.Immersive"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <activity
            android:name="com.github.ss.QuickToggleShortcut"
            android:excludeFromRecents="true"
            android:label="@string/quick_toggle"
            android:process=":bg"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.CREATE_SHORTCUT" />
            </intent-filter>
        </activity>

        <service
            android:name="com.github.ss.bg.TileService"
            android:directBootAware="true"
            android:icon="@drawable/ic_money"
            android:label="@string/quick_toggle"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:process=":bg"
            tools:targetApi="n">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.github.ss.tasker.ActionListener"
            android:directBootAware="true"
            android:process=":bg"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.FIRE_SETTING" />
            </intent-filter>
        </receiver> <!-- <meta-data -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7592917484201943~4083820671" />
    </application>

</manifest>